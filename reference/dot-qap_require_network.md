# Require the network package

Checks whether the network package is installed and stops with an
informative error if it is unavailable.

## Usage

``` r
.qap_require_network()
```

## Value

Invisibly returns nothing. Called for its side effect of validating that
network is available.
