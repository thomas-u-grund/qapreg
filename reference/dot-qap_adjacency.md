# Extract an adjacency matrix from a network object

Converts a network object to an adjacency matrix, attempting to retain
missing dyads where supported.

## Usage

``` r
.qap_adjacency(net)
```

## Arguments

- net:

  A network object.

## Value

An adjacency matrix for `net`.
