% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qapreg.R
\name{compare_netlogit}
\alias{compare_netlogit}
\title{Compare qapreg and netlogit estimates}
\usage{
compare_netlogit(
  object,
  net,
  reps = NULL,
  dyadic_covariates = NULL,
  missing_dyads = NULL,
  seed = NULL
)
}
\arguments{
\item{object}{An object of class \code{"qapreg"}, usually returned by \code{\link[=qapreg]{qapreg()}} or
\code{\link[=qaplogit]{qaplogit()}}.}

\item{net}{The original \pkg{network} object used to fit \code{object}.}

\item{reps}{Optional number of permutations for the \code{\link[sna:netlogit]{sna::netlogit()}}
comparison. Defaults to \code{object$reps}.}

\item{dyadic_covariates}{Optional named list of dyadic covariate matrices.
These should match the covariates used to fit \code{object}.}

\item{missing_dyads}{Optional missing-dyad handling rule for the comparison.
Defaults to \code{object$missing_dyads}.}

\item{seed}{Optional random seed used before fitting the comparison model.}
}
\value{
A data frame comparing terms, coefficients, and two-sided QAP
p-values. The full \code{\link[sna:netlogit]{sna::netlogit()}} object is stored as the \code{"netlogit"}
attribute.
}
\description{
Compares coefficients and two-sided QAP p-values from a fitted \code{qapreg} object
with an equivalent \code{\link[sna:netlogit]{sna::netlogit()}} model.
}
\examples{
\dontrun{
fit <- qaplogit(net, y ~ Grade_i + Grade_j, reps = 100)
compare_netlogit(fit, net = net, reps = 100)
}

}
\seealso{
\code{\link[=qap_netlogit_compare]{qap_netlogit_compare()}}, \code{\link[=qapreg]{qapreg()}}, \code{\link[=qaplogit]{qaplogit()}}
}
