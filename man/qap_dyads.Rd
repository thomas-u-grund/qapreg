% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qapreg.R
\name{qap_dyads}
\alias{qap_dyads}
\title{Create dyadic data from a network object}
\usage{
qap_dyads(
  net,
  directed = NULL,
  dyadic_covariates = NULL,
  missing_dyads = c("omit", "fail", "zero")
)
}
\arguments{
\item{net}{A \pkg{network} object.}

\item{directed}{Logical; whether to treat the network as directed. If \code{NULL},
the directedness is taken from \code{net} using \code{\link[network:is.directed]{network::is.directed()}}.}

\item{dyadic_covariates}{Optional named list of dyadic covariate matrices.
Each matrix must have dimensions \code{n} by \code{n}, where \code{n} is the number of
vertices in \code{net}.}

\item{missing_dyads}{Character string specifying how missing dyads in the
outcome network are handled. \code{"omit"} drops missing outcome dyads, \code{"fail"}
stops if missing dyads are present, and \code{"zero"} treats missing dyads as
zero.}
}
\value{
A data frame with one row per dyad and at least the columns \code{y}, \code{i},
and \code{j}. The returned data frame also contains attributes including \code{dyads},
\code{ymat}, \code{n}, \code{directed}, \code{missing_dyads}, and \code{observed_mask}.
}
\description{
Converts a \pkg{network} object into a dyadic data frame suitable for QAP
regression. The outcome variable is named \code{y}. Vertex attributes are added as
sender and receiver variables using \verb{_i} and \verb{_j} suffixes. Edge attributes and
user-supplied dyadic covariates are added as dyadic variables.
}
\examples{
\dontrun{
library(network)

net <- network::network(matrix(rbinom(25, 1, 0.2), 5, 5), directed = TRUE)
dat <- qap_dyads(net)
head(dat)
}

}
\seealso{
\code{\link[=qapreg]{qapreg()}}, \code{\link[=qaplogit]{qaplogit()}}
}
