% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qapreg.R
\name{qapreg}
\alias{qapreg}
\title{Quadratic Assignment Procedure regression}
\usage{
qapreg(
  net,
  formula,
  reps = 100,
  directed = NULL,
  dyadic_covariates = NULL,
  missing_dyads = c("omit", "fail", "zero"),
  na_action = c("omit", "fail", "pass"),
  fit_fun = stats::glm,
  fit_args = list(family = stats::binomial()),
  seed = NULL,
  save_permutations = TRUE,
  save_permutation_ses = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{net}{A \pkg{network} object.}

\item{formula}{A model formula. The response should usually be \code{y}, the
internally generated dyadic outcome. Predictors may include vertex
attributes with \verb{_i} and \verb{_j} suffixes, edge attributes, and supplied dyadic
covariates.}

\item{reps}{Number of QAP permutations.}

\item{directed}{Logical; whether to treat the network as directed. If \code{NULL},
the directedness is taken from \code{net} using \code{\link[network:is.directed]{network::is.directed()}}.}

\item{dyadic_covariates}{Optional named list of dyadic covariate matrices.
Each matrix must have dimensions \code{n} by \code{n}.}

\item{missing_dyads}{Character string specifying how missing outcome dyads are
handled. One of \code{"omit"}, \code{"fail"}, or \code{"zero"}.}

\item{na_action}{Character string specifying how missing values in model
variables are handled. One of \code{"omit"}, \code{"fail"}, or \code{"pass"}.}

\item{fit_fun}{Model-fitting function. Defaults to \code{\link[stats:glm]{stats::glm()}}.}

\item{fit_args}{Named list of additional arguments passed to \code{fit_fun}.}

\item{seed}{Optional random seed used before generating permutations.}

\item{save_permutations}{Logical; if \code{TRUE}, store permuted coefficient
estimates in the returned object.}

\item{save_permutation_ses}{Logical; if \code{TRUE}, store model-based standard
errors from each permutation in the returned object.}

\item{verbose}{Logical; if \code{TRUE}, print progress messages during
permutations.}
}
\value{
An object of class \code{"qapreg"}, a list containing observed
coefficients, model-based standard errors, QAP standard errors,
permutation covariance matrix, one-sided and two-sided QAP p-values, the
observed fitted model, and information about the QAP settings.
}
\description{
Fits a regression model to dyadic network data and evaluates coefficient
significance using node-label permutations under the Quadratic Assignment
Procedure (QAP).
}
\details{
QAP inference preserves network dependence by repeatedly permuting node labels
and refitting the model to the permuted outcome network. P-values are computed
from the empirical permutation distribution using a plus-one correction.

The function is deliberately flexible: by changing \code{fit_fun} and \code{fit_args},
users can fit models other than logistic regression, provided the fitted model
supports \code{\link[stats:coef]{stats::coef()}} and, ideally, \code{\link[stats:vcov]{stats::vcov()}}.
}
\examples{
\dontrun{
library(network)

mat <- matrix(rbinom(100, 1, 0.2), 10, 10)
diag(mat) <- 0
net <- network::network(mat, directed = TRUE)

fit <- qapreg(net, y ~ i + j, reps = 50, verbose = FALSE)
print(fit)
}

}
\seealso{
\code{\link[=qaplogit]{qaplogit()}}, \code{\link[=qap_dyads]{qap_dyads()}}, \code{\link[=compare_netlogit]{compare_netlogit()}}
}
