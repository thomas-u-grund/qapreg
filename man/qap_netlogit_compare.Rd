% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qapreg.R
\name{qap_netlogit_compare}
\alias{qap_netlogit_compare}
\title{Compare a QAP specification with sna::netlogit}
\usage{
qap_netlogit_compare(
  net,
  formula,
  reps = 100,
  dyadic_covariates = NULL,
  missing_dyads = c("omit", "fail", "zero"),
  seed = NULL
)
}
\arguments{
\item{net}{A \pkg{network} object.}

\item{formula}{A model formula using the internally generated outcome \code{y}.}

\item{reps}{Number of QAP permutations.}

\item{dyadic_covariates}{Optional named list of dyadic covariate matrices.}

\item{missing_dyads}{Character string specifying how missing outcome dyads are
handled. One of \code{"omit"}, \code{"fail"}, or \code{"zero"}.}

\item{seed}{Optional random seed used before fitting the comparison model.}
}
\value{
The fitted object returned by \code{\link[sna:netlogit]{sna::netlogit()}}.
}
\description{
Fits an equivalent QAP logistic regression using \code{\link[sna:netlogit]{sna::netlogit()}} for
comparison with this package's implementation.
}
\details{
This function is intended primarily as a diagnostic and validation aid.
Because \code{\link[sna:netlogit]{sna::netlogit()}} operates on full adjacency matrices, exact
equivalence is easiest when there are no missing dyads or when
\code{missing_dyads = "zero"} is used.
}
\examples{
\dontrun{
library(network)
library(sna)

mat <- matrix(rbinom(100, 1, 0.2), 10, 10)
diag(mat) <- 0
net <- network::network(mat, directed = TRUE)

qap_netlogit_compare(net, y ~ i + j, reps = 50)
}

}
\seealso{
\code{\link[=compare_netlogit]{compare_netlogit()}}, \code{\link[=qapreg]{qapreg()}}, \code{\link[=qaplogit]{qaplogit()}}
}
